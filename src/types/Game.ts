export enum GameStatus {
  READY = 'READY',
  PLAYING = 'PLAYING',
  END = 'END',
}

export enum PlayPhase {
  STARTING = 'STARTING',
  FIRST_HALF = 'FIRST_HALF',
  SECOND_HALF = 'SECOND_HALF',
  LAST = 'LAST',
}
